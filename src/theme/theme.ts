"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#d9e2e7",
        },
        secondary: {
            main: "#154b77",
            contrastText: "#afc9db",
        },
    },
});

export default theme;
